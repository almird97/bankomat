export declare class LoginInfoDto {
    clientId: number;
    pin: string;
    novac: number;
    aktivan: string;
    constructor(clientId: number, pin: string, novac: number, aktivan: string);
}
